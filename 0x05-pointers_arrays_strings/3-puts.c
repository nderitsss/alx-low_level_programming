#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * _puts - print a string to stdout
 * @ str : a string
 *
 * Return Always 0
 */
void _puts(char *str)
{
	str = (char *)str;
	puts(str);
}
