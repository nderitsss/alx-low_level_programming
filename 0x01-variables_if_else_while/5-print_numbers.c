#include <stdio.h>
/**
 * main - prints numbers betweenzero and ten
 *
 * Return : Always success 0
 */
int main(void)
{
	int n = 0;

	for (n = 0; n < 10; n++)
	{
		printf("%d", n);
	}
	putchar('\n');
	return (0);
}
