#include "main.h"
#include <stdio.h>
/**
 *print_last_digit - function
 *
 * Return Always (Success) 0
 */
int print_last_digit(int n)
{
	if (n < 0)
	{
		_abs(n)
	}
	int m = n % 10;
	
	_putchar(m + '0');
	return (0);
}
