#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - multiplies two integers
 * @a : int 1
 * @b : int 2
 *
 * Result : mul 
 */
int main(int argc, char **argv)
{
	int a;
	int b;
	void(argc);
	void(argv);

	if (argc < 3)
		printf("Error");
		return (1);
	printf("%d", a * b);
	return (0);
}
