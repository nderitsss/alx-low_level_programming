#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - multiplies two integers
 * @a : int 1
 * @b : int 2
 *
 * Result : mul 
 */
int mul(int a, int b)
{
	if (argc(mul) < 3)
	{
		printf("Error");
		return 1;
	}
	else
	{
		return (a * b);
	}

}
