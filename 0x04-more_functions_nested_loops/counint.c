#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * count - how many integers a functions has
 *
 * Return Always 0
 */
int count(int n)
{
	int count = 0;
	while ( n / 10 > 10)
	{

		count++;
	}
}
